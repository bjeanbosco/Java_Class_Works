public class ternaryOperatorsecond {
    public static void main(String args[]) {
        int a = 10;// variable
        int b = 5;// varaible
        int min = (a < b) ? a : b;// ternary operator ? work as if and : works as else
        System.out.println(min);
    }
}
